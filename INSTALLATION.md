# ИНСТРУКЦИЯ ПО УСТАНОВКЕ ЗАВИСИМОСТЕЙ

## НУЖНЫЕ ПАКЕТЫ:

### Основные зависимости:
1. **PySide6** - для GUI интерфейса ✅ УСТАНОВЛЕН
2. **pynput** - для записи и воспроизведения ввода (клавиатура и мышь) ✅ УСТАНОВЛЕН

### Для Windows (рекомендуется для игр):
```cmd
pip install PySide6 pynput
```

### Для Linux/macOS:
```bash
# Ubuntu/Debian:
sudo apt update
sudo apt install python3-pyside6 python3-pynput

# Или через pip (если разрешено):
pip install PySide6 pynput --break-system-packages
```

## ТЕКУЩИЙ СТАТУС:
✅ **PySide6**: Успешно установлен
✅ **pynput**: Успешно установлен (но требует GUI для работы)

## ПРОВЕРКА УСТАНОВКИ:

```bash
# Проверка PySide6:
python3 -c "import PySide6; print('PySide6: OK')"

# Проверка pynput (только с GUI):
python3 -c "import pynput; print('pynput: OK')"
```

## ЕСЛИ ПРОБЛЕМЫ С УСТАНОВКОЙ:

### Проблема 1: "externally-managed-environment"
**Решение:** Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
pip install PySide6 pynput
```

### Проблема 2: Права доступа на Linux
**Решение:** Используйте sudo:
```bash
sudo pip install PySide6 pynput
```

### Проблема 3: pynput не работает на Linux без GUI
**Нормально:** pynput требует графический интерфейс. Для тестирования можно использовать только Windows.

## АЛЬТЕРНАТИВНЫЙ ВАРИАНТ:

Если не хотите устанавливать ничего, можно использовать готовые исполняемые файлы:
```bash
# Создать .exe файл (требуется pyinstaller)
pip install pyinstaller
pyinstaller --onefile --windowed "imba s kameroy.py"
```

## МИНИМАЛЬНЫЕ ТРЕБОВАНИЯ:
- Python 3.7+
- Windows 10/11 (рекомендуется для игр)
- или Linux/macOS с графическим интерфейсом

## ЗАПУСК ПОСЛЕ УСТАНОВКИ:
```bash
python "imba s kameroy.py"
```

Если видите окно программы - всё установлено правильно! 🎉